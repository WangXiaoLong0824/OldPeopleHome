<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jk.dao.TurnHomeMapper">

    <select id="findTurnHomeCount" parameterType="com.jk.entity.TurnHome" resultType="java.lang.Long">
       select count(*)
from t_turnhome t ,t_oldpeople o, t_person p ,t_room r,t_storey s,t_dorm d
where t.old_id=o.old_id and t.person_id=p.person_id and
r.room_id=t.room_id and s.storey_id=t.storey_id and d.dorm_id=t.dorm_id
    </select>

    <select id="findTurnHome" resultType="com.jk.entity.TurnHome">
        <bind name="key_off" value="(currPage - 1)*pageSize"/>
        select t. turn_id turnId ,t.createtime ,t.because,o.old_name oldName,p.person_name personName ,r.room_name roomName,
        d.dorm_name dormName ,s.storey_name storeyName
        from t_turnhome t ,t_oldpeople o, t_person p ,t_room r,t_storey s,t_dorm d
        where t.old_id=o.old_id and t.person_id=p.person_id and
        r.room_id=t.room_id and s.storey_id=t.storey_id and d.dorm_id=t.dorm_id
         limit ${key_off},${pageSize}
    </select>

</mapper>